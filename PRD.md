# 拼好机官网 Landing Page PRD

> 文档版本：v0.2 Draft  
> 文档状态：待审批，审批前不进入开发  
> 正式品牌：拼好机 / Pinhaoji Cloud（英文品牌名须经商标与法务核验后最终确认）  
> 官方域名：pinhaoji1.cn  
> 目标上线平台：EdgeOne Makers（原 EdgeOne Pages，优先兼容其全栈托管与 Functions 能力；须完成目标市场访问质量、数据处理位置与合规评估）  
> 首版语言策略：中文优先，面向海外客户提供英文版本；技术架构支持持续国际化

## 1. 项目概述

为拼好机建设面向全球客户、以海外市场为优先的云计算品牌官网首版。拼好机以高性价比、高可用性和高稳定性的云基础设施服务开发者、成长型团队及企业客户。首版以 Landing Page 为核心，用可核验的产品能力、清晰的价格逻辑、透明的服务边界和明确的转化入口建立专业可信形象，并为后续扩展产品详情、价格、文档、控制台和客户案例预留信息架构。

设计参考 AWS、Cloudflare、Vercel、DigitalOcean 等国际云厂商的成熟表达方式，但不复制其版式、文案或品牌资产。页面应体现拼好机“以合理成本获得可靠云能力”的品牌判断，具备国际云服务品牌的可信度和技术感，同时保持较低的认知负担。

## 2. 背景与问题

当前仓库为空，尚无既有品牌规范、内容资产或技术约束。首版需要同时解决以下问题：

- 让首次访问者在首屏快速理解公司提供什么、适合谁、下一步做什么。
- 在缺少大规模品牌认知的阶段，通过产品能力、基础设施、服务承诺和真实证据建立信任。
- 为销售咨询、产品试用或控制台注册提供稳定的转化路径。
- 形成可复用的视觉与组件基础，避免未来扩站时推倒重来。
- 满足 `pinhaoji1.cn` 的域名规范、静态生成（SSG）优先、SEO、性能、可访问性、移动端体验与适用法律要求。

## 3. 产品目标

### 3.1 核心目标

1. 建立清晰、可信、面向海外优先的国际化云计算品牌形象。
2. 帮助访客在 10 秒内理解核心产品与差异化价值。
3. 将高意向访客引导至“立即使用”或“联系销售”转化路径。
4. 为 Google、Bing、百度及主要社交平台提供完整、可索引的品牌入口。
5. 建立后续产品页、定价页、文档站和客户案例的设计及工程基础。

### 3.2 首版成功指标

上线后 30 天建立基线，后续按真实流量调整目标值：

- 首屏主 CTA 点击率可被独立统计。
- 联系销售表单提交成功率和错误率可被统计。
- Lighthouse（生产环境、移动端）Performance ≥ 90。
- Lighthouse Accessibility、Best Practices、SEO ≥ 95。
- Core Web Vitals 达到 Good：LCP ≤ 2.5s、INP ≤ 200ms、CLS ≤ 0.1（以真实用户数据为最终依据）。
- 主要页面在 360px、768px、1440px、1920px 宽度下无横向滚动、遮挡或不可操作控件。

### 3.3 非目标

首版不包含：

- 云产品控制台、账户、计费与资源管理系统。
- 完整在线购买、支付、工单与客户后台。
- 完整文档中心、博客 CMS 或多层级产品详情站。
- 实时产品价格计算器。
- 未经业务确认的客户 Logo、认证资质、节点数量、SLA、性能数据或“全球覆盖”等表述。

## 4. 目标用户

### 4.1 开发者与技术负责人

关注部署速度、API、弹性、网络质量、可观测性、文档质量和价格透明度。希望快速判断平台是否适合新项目或迁移。

### 4.2 创业公司与成长型团队

关注成本、上线效率、稳定性、技术支持和随业务扩展的能力。需要低摩擦试用入口和容易理解的产品组合。

### 4.3 企业 IT 与采购决策者

关注服务可用性、安全合规、交付能力、服务范围、合同与售后支持。更倾向通过联系销售进入转化。

### 4.4 海外业务与跨境团队

关注海外访问质量、区域可用性、跨境运维效率、结算便利性和英文支持。首版内容应优先回答“服务在哪些区域可用、如何接入、费用如何估算、出现问题如何获得支持”。

## 5. 品牌定位与内容原则

### 5.1 品牌定位与主张

**拼好机：让可靠云基础设施更具性价比。**

品牌定位：面向全球客户、以海外市场为优先的云厂商，为开发者、成长型团队和企业提供高性价比、高可用性、高稳定性的云基础设施。

对外表达应围绕“成本可控、服务可用、运行稳定”展开。所有可用性、性能、覆盖区域、价格、客户和认证数据必须有业务或技术凭证；示例数字只能在设计稿中标注“示例/待核验”，上线前必须替换为真实数据。不得使用“全球领先”“绝对安全”“100% 可用”等无法证明或容易造成误解的绝对化表述。

### 5.2 品牌性格

- 专业、克制、可信。
- 技术先进，但不堆砌术语。
- 清晰直接，避免空泛宏大叙事。
- 对开发者友好，同时能让非技术决策者理解成本、风险和服务边界。

### 5.3 文案原则

- 标题优先描述客户结果，正文再解释技术能力。
- 一屏只传达一个主要观点。
- 所有数字、SLA、地域、资质、价格和客户背书必须可验证，并注明统计口径、时间范围和适用条件。
- CTA 使用明确动作，如“开始使用”“联系销售”“查看产品”，不使用含糊的“了解更多”作为主要 CTA。
- 首版中文文案完成后保留国际化键值，不在组件中硬编码大段文本。

### 5.4 合规表达原则

- 遵守中国《广告法》《反不正当竞争法》《个人信息保护法》《数据安全法》《网络安全法》及目标海外市场的适用规定。
- “低价”“高性价比”“高可用”“高稳定”等表述必须有明确口径、适用范围和证据链；不得通过对比性贬低、虚构原价、虚假限时或误导性排名制造紧迫感。
- 价格、SLA、退款、数据存储区域、支持时段和服务边界以正式条款为准，营销页面不得作出超出合同的承诺。

## 6. 信息架构

### 6.1 首版路由

| 路由 | 页面 | 优先级 | 说明 |
| --- | --- | --- | --- |
| `/` | 拼好机官网首页 | P0 | 首版主要 Landing Page，部署于 `https://pinhaoji1.cn/` |
| `/privacy` | 隐私政策 | P0 | 表单和统计上线所需 |
| `/terms` | 服务条款 | P0 | 基础法律页面，占位内容需正式审核 |
| `/en` | 英文首页 | P1 | 架构预留；是否随首版上线待审批 |
| `/404` | 404 状态 | P1 | 提供返回首页与联系入口 |

### 6.2 顶部导航

- 拼好机 Logo 与品牌名称。
- 产品：锚点或菜单，首版可跳转至产品能力区。
- 解决方案：锚点或菜单，首版可跳转至典型场景区。
- 基础设施：跳转至网络与地域能力区。
- 定价：首版若无真实定价数据，则不展示或改为“联系咨询”。
- 文档：仅在已有可访问文档地址时出现。
- 登录：仅在已有控制台地址时出现。
- 主要 CTA：开始使用。
- 次要 CTA：联系销售。
- 语言切换：英文版随首版上线时显示；否则隐藏但保留实现能力。

## 7. 首页内容结构

### 7.1 全局公告条（可选）

用于真实的产品发布、区域上线或活动信息。没有有效内容时不显示，不以假消息填充空间。

### 7.2 Hero 首屏

目标：在 10 秒内回答“这是什么、为什么值得关注、下一步做什么”。

内容：

- 品牌名称必须是首屏强信号。
- H1 使用明确品类或价值主张，不使用抽象口号独占主标题。
- 一段不超过两行的补充说明。
- 主 CTA：“开始使用”或“申请试用”（以真实开通流程为准）。
- 次 CTA：“联系销售”。
- 一组短可信点，如按需扩展、稳定网络、技术支持；仅展示真实能力。
- 真实产品界面、基础设施可视化或可验证的网络节点视觉作为主视觉；不使用模糊机房图库充当产品证明。

交互：

- 首屏动效轻量、可被 `prefers-reduced-motion` 关闭。
- 首屏高度在常见桌面和移动视口中保留下一内容区提示。
- CTA 在移动端不被折叠或装饰遮挡。

### 7.3 信任与事实条

用一组可核验事实快速建立信任，例如已开放区域、服务状态、价格透明度、技术支持渠道或合规资质。若尚无真实数据，使用产品类别入口或“能力持续建设中”代替，不展示虚构数字。可在内部设计稿中使用如下占位格式，发布前必须替换并完成核验：`示例：可用性目标 99.95%（待核验）`、`示例：覆盖 X 个区域（待核验）`。

### 7.4 核心产品能力

首版建议最多展示 4 项，避免成为产品目录：

1. 云服务器 / 弹性计算：按需使用、成本可控，适合网站、API 和业务服务。
2. 裸金属或高性能计算：面向对性能、隔离性有明确要求的工作负载。
3. 对象存储与数据保护：提供可扩展的数据存储与备份能力，具体持久性指标以正式产品说明为准。
4. 云网络、CDN 或安全能力：改善海外访问体验并降低网络波动风险，实际区域和能力以已上线产品为准。

每项包含产品名、一句话结果导向描述、2 至 3 个能力点和明确入口。最终产品集合需按公司实际可售能力确认。

### 7.5 平台差异化能力

用一个强叙事区解释 3 个真正可证明的差异点，候选方向：

- 高性价比：用透明的计费逻辑和清晰的资源规格，帮助团队控制云成本。
- 高可用性：通过冗余设计、健康检查、故障切换和服务状态披露降低单点风险；具体 SLA 仅展示已签署并可履约的承诺。
- 高稳定性：关注网络、计算、存储和发布流程的长期运行质量，提供可观测性、变更记录和故障沟通机制。

该区域避免泛用图标卡片堆叠，优先采用指标对照、架构示意或产品操作状态来表达。

### 7.6 基础设施与覆盖

- 展示真实可用区域、数据中心或网络覆盖。
- 地图作为内容辅助，不以不可访问的纯 Canvas 承载关键信息。
- 地域列表提供文本等价内容。
- 不上线虚构节点；尚未开放的地点必须标记“即将上线”。

### 7.7 解决方案 / 使用场景

建议选取 3 个高价值场景，例如：

- 网站、应用与 API 托管。
- 游戏、音视频或实时业务加速。
- 企业上云、混合云与灾备。

每个场景说明问题、对应能力和预期结果，不做行业口号集合。

### 7.8 开发者体验

在已有 API、CLI、Terraform Provider 或文档时展示真实代码片段与文档入口。若尚未具备这些能力，则以控制台工作流或部署步骤替代，不展示不可用的伪代码接口。

### 7.9 客户证明

优先级顺序：真实客户案例 > 带授权客户 Logo > 可验证评价 > 暂不展示。客户引用需要姓名、角色、公司和授权状态，不使用匿名虚构推荐语。

### 7.10 FAQ

首版建议 5 至 7 个问题，覆盖试用、计费、迁移、技术支持、数据安全、退款或 SLA。FAQ 内容必须与真实服务政策一致，并输出 FAQ 结构化数据时遵守搜索引擎规范。

### 7.11 末尾 CTA

以“开始使用”为主动作，“联系销售”为次动作。对于尚未开放自助注册的阶段，主 CTA 统一改为“申请试用”，避免死链或虚假流程。

### 7.12 Footer

- 产品、解决方案、资源、公司与法律链接分组。
- 品牌名称“拼好机”、公司主体、联系邮箱、版权与备案信息（如适用）。域名统一使用 `pinhaoji1.cn`，不得混用未确认的备用域名。
- 社交媒体仅展示真实运营账号。
- 状态页仅在存在可用地址时展示。

## 8. 视觉设计规范

### 8.1 设计方向

- 整体气质接近国际云平台：高信息密度、强层级、精确网格、克制动效。
- 以明亮中性色为主要画布，搭配深色技术展示区；避免全站单一深蓝或紫色渐变。
- 品牌强调色暂定为高识别度冷色，最终根据 Logo 和品牌资产确定。
- 强调真实产品、基础设施和数据表达，不依赖抽象光球、模糊渐变或通用图库。
- 卡片圆角不超过 8px；页面大区块不做悬浮卡片，避免卡片嵌套。
- 展示字体与正文均选用具备中英文覆盖和良好网页性能的字体方案，不按视口宽度缩放字号，不使用负字距。

### 8.2 组件来源

- **Magic UI**：用于轻量的文字、边框、网格或数据展示动效。
- **Aceternity UI**：用于精选的聚光、导航、时间线或基础设施视觉交互。
- **shadcn/ui + Radix Primitives**：建议作为基础可访问组件层，用于 Dialog、Navigation Menu、Accordion、Form 等；Magic UI 与 Aceternity UI 不承担全部基础交互。
- **Lucide React**：统一功能性图标。

使用原则：按需复制和重构组件，统一到项目 Design Tokens；不把两个组件库的默认样式直接混用。每个引入组件需检查许可证、依赖、键盘操作、移动端和 reduced-motion 行为。

### 8.3 动效原则

- 动效服务于状态变化、阅读顺序和空间关系。
- 首屏不使用高成本连续粒子背景或阻塞主线程的长时间动画。
- 主要转场建议 160 至 400ms；滚动触发内容只执行一次或保持低频。
- 所有非必要动画支持系统减少动态效果设置。
- 低性能设备与移动端采用降级效果。

## 9. 功能需求

### 9.1 联系销售表单

建议字段：姓名、工作邮箱、公司、需求类型、预计规模、需求描述、隐私同意。

要求：

- 前后端双重校验，错误信息具体且可被辅助技术读取。
- 提交时有 loading、成功、失败和防重复状态。
- 服务端处理优先使用 EdgeOne Makers 的 Functions 能力（Next.js API Routes 自动部署为 Cloud Functions，或 `edge-functions` 目录的 Edge Functions），不在浏览器暴露邮件或 CRM 密钥。
- 支持垃圾提交防护；优先使用无感或低干扰方案。
- 数据去向、保存周期和删除流程在隐私政策中说明。
- CRM、邮件或数据库接入方式在技术设计阶段确认。

### 9.2 CTA 与外部链接

- 所有“开始使用”“登录”“文档”“状态页”地址通过配置管理。
- 未配置的入口不得渲染为可点击死链。
- 外部链接明确安全属性；关键转化事件具有统一命名。

### 9.3 国际化

- URL 使用路径前缀，如 `/` 与 `/en`，不依赖客户端即时翻译。
- 页面元数据、Open Graph、JSON-LD、表单和错误信息均可本地化。
- 使用 `hreflang` 和 canonical 避免重复内容。
- 中文和英文允许内容长度不同，组件不能依赖固定字符数。

## 10. 技术方案

### 10.1 推荐技术栈

- **Framework**：Next.js 最新稳定版，App Router，优先 React Server Components。
- **Language**：TypeScript，开启严格模式。
- **UI / Styling**：Tailwind CSS 最新稳定版、CSS Variables、shadcn/ui、Magic UI、Aceternity UI。
- **Animation**：Motion；仅在必要处使用，避免重复引入动效库。
- **Icons**：Lucide React。
- **Internationalization**：`next-intl` 或同等成熟方案，技术设计阶段根据最新版兼容性确认。
- **Forms**：React Hook Form + Zod；服务端使用 Next.js API Routes（EdgeOne Makers 自动部署为 Cloud Functions）或 Edge Functions。
- **Quality**：ESLint、Prettier、TypeScript、Vitest、Testing Library、Playwright。
- **Deployment**：EdgeOne Makers（原 EdgeOne Pages），生产/预览双环境自动部署（Git 集成或 EdgeOne CLI）；正式域名绑定 `pinhaoji1.cn`，并配置 HTTPS、DNS、重定向和域名 canonical。
- **Monitoring**：EdgeOne Makers 内置访问指标与数据分析；是否接入第三方分析待审批。

方案取舍：首版以 Next.js App Router 的 SSG 为默认方案，适合同时承载中文/英文、法律页面、表单和后续产品扩展；EdgeOne Makers 对全栈框架深度集成，Next.js 的 SSR/SSG/ISR 与 API Routes（自动部署为 Cloud Functions）可直接使用，无静态导出限制。若最终确认网站长期以文档和营销内容为主、交互较少，可评估 Astro 静态站点作为 SEO 优先的替代方案，其轻量动态能力由 Edge Functions 补充。无论采用哪种框架，公开页面必须在构建时输出完整 HTML，不依赖客户端渲染才能出现核心内容。

依赖版本在项目初始化当日锁定最新稳定、互相兼容的版本，不使用 canary、beta 或 experimental 版本作为生产基线，除非有书面技术决策记录。

### 10.2 架构原则

- 首页、产品能力、解决方案、FAQ 等公开内容采用 SSG（Static Site Generation）优先，确保抓取稳定、首屏快速和部署成本可控；只有表单提交和确需实时的数据使用服务端能力。
- 如后续内容更新频繁，可采用 ISR 或构建时内容同步，但不得为了动态化而牺牲主要内容的可抓取性。
- 服务端能力优先使用 EdgeOne Makers Functions：Next.js API Routes 自动部署为 Cloud Functions（Node.js）；轻量请求（表单提交、GEO 识别、重定向、响应头）可使用 `edge-functions` 目录的 Edge Functions（仅 JavaScript/ES2023+，受代码包 5MB、请求 body 1MB、CPU 时间 200ms 限制）。
- 尽可能减少客户端组件、JavaScript 体积和 hydration 范围。
- 内容数据与页面组件分离，为未来 CMS 或多页面扩展预留清晰边界。
- Design Tokens 统一颜色、排版、间距、圆角、阴影和动效。
- 图片使用 `next/image`（需确认 EdgeOne 构建环境的图片优化兼容性，必要时配置自定义 loader 或构建时压缩），字体使用 `next/font` 或经过性能验证的自托管方案。
- 环境变量区分 Preview 与 Production，并在 EdgeOne Makers 环境管理中配置；生产域名、表单接收地址和统计开关必须单独配置。
- 不将业务密钥、CRM Token 或邮件服务密钥暴露为 `NEXT_PUBLIC_*`。

### 10.3 建议目录

```text
app/
  [locale]/
  api/
components/
  ui/
  magicui/
  aceternity/
  sections/
content/
i18n/
lib/
public/
styles/
tests/
```

最终目录以初始化时所选国际化路由方案为准。

## 11. SEO 与分享

- 每种语言提供独立 title、description、canonical 与 Open Graph 信息；中文首页建议围绕“拼好机、海外云服务器、高性价比云计算”等真实业务词优化，英文页使用对应自然表达，不堆砌关键词。
- 官方主域名固定为 `https://pinhaoji1.cn`，统一 HTTP 到 HTTPS、裸域与 `www`（如启用）的跳转规则，并避免 Preview 域名被搜索引擎收录。
- SSG 页面在构建时输出完整 HTML、结构化数据和可抓取链接；为首页、产品、解决方案、法律页面生成稳定 URL。
- 输出 Organization、WebSite、Product/Service 等真实适用的 JSON-LD；不伪造评分或评价。
- 提供 `sitemap.xml`、`robots.txt`、favicon、manifest 和 1200×630 分享图。
- 页面只有一个语义清晰的 H1，标题层级连续。
- 重要内容服务端输出，不依赖动画后才进入 DOM。
- 404、重定向、尾斜杠、域名 canonical、`sitemap.xml` 和 `robots.txt` 策略在上线前统一，并提交主要搜索引擎站长平台。

## 12. 可访问性

- 以 WCAG 2.2 AA 为目标。
- 全站键盘可操作，焦点样式清晰，菜单、表单、Accordion 和 Dialog 行为符合语义。
- 正文和控件达到颜色对比度要求，不仅以颜色表达状态。
- 图像提供合适替代文本；装饰图像对辅助技术隐藏。
- 地图、图表和动效提供文本等价内容。
- 触控目标最小 44×44 CSS px。
- 支持 200% 缩放，不发生内容遮挡或功能丢失。

## 13. 性能与安全

### 13.1 性能预算

- 首屏关键图片优先加载，非关键媒体懒加载。
- 首屏避免自动播放视频；确需视频时提供 poster、压缩版本和移动端降级。
- 单页第三方脚本保持最少，营销脚本须经审批。
- JavaScript bundle 在开发阶段使用构建分析工具检查，具体预算在首个实现基线后锁定。
- 动效组件不应造成持续高 CPU/GPU 占用或明显布局抖动。

### 13.2 安全与隐私

- 配置 CSP、HSTS、`X-Content-Type-Options`、`Referrer-Policy` 等响应头。
- 表单接口具备速率限制、输入校验、长度限制和日志脱敏。
- 统计与 Cookie 行为根据中国《个人信息保护法》《数据安全法》《网络安全法》及目标海外市场适用法律确定；涉及跨境传输、第三方处理或境外部署时，须由法务确认处理依据、告知与同意机制。
- 联系销售表单遵循最小必要原则，不采集与咨询无关的敏感个人信息；隐私政策明确处理目的、保存期限、数据主体权利、委托处理方和删除渠道。
- 不在前端提交个人数据到未经批准的第三方；如使用 EdgeOne Makers 内置分析、邮件服务或 CRM，需完成供应商安全评估与数据处理协议审查。
- 若网站面向中国境内公众提供服务，按实际业务范围评估 ICP 备案、公安联网备案、域名实名和网络安全等要求；未完成前不得对外宣称已具备相关资质。
- 依赖在合并前进行漏洞扫描和许可证检查。

## 14. 埋点方案

首版建议事件：

| 事件 | 触发条件 | 关键属性 |
| --- | --- | --- |
| `cta_click` | 点击主要或次要 CTA | `location`, `label`, `destination` |
| `nav_click` | 点击导航 | `item`, `destination` |
| `sales_form_start` | 首次聚焦表单 | `source` |
| `sales_form_submit` | 表单提交成功 | `source`, `need_type` |
| `sales_form_error` | 提交失败 | `error_type`，不得含个人数据 |
| `language_switch` | 切换语言 | `from`, `to` |

分析平台、用户同意策略和数据保留期限需在开发前确认。

## 15. 验收标准

### 15.1 内容与视觉

- 页面结构与本 PRD 已审批内容一致。
- 所有对外事实、数据、客户、认证和链接由业务方确认。
- Magic UI 与 Aceternity UI 组件被统一设计，而非保留互相冲突的 Demo 风格。
- 桌面、平板、移动端无内容重叠、横向滚动、文字溢出或跳动布局。
- 页面在禁用动画和弱性能设备策略下仍完整可用。

### 15.2 功能

- 所有导航、锚点、CTA、外部链接和表单路径可用。
- 表单成功、失败、超时、重复提交和校验错误状态均经过测试。
- 未配置的控制台、文档、状态页链接不会产生死链。
- Preview 与 Production 环境配置隔离。

### 15.3 工程质量

- TypeScript、lint、单元测试和生产构建通过。
- Playwright 覆盖首页主要路径、导航、表单与移动端菜单。
- 使用真实浏览器检查 360px、768px、1440px、1920px 视口。
- Lighthouse 指标满足本 PRD 目标，未满足项有记录和审批。
- EdgeOne Makers 预览环境可供验收，合并后可稳定部署 Production。

## 16. 实施阶段与交付物

### 阶段 0：需求与品牌确认

交付物：审批后的 PRD、品牌资料清单、内容事实表、站点路由与 CTA 地址。

### 阶段 1：视觉方向与内容框架

交付物：首页线框、视觉方向稿、Design Tokens、最终页面文案、响应式策略。

### 阶段 2：工程实现

交付物：Next.js 项目、首页与法律页面、响应式组件、表单、SEO、国际化基础。

### 阶段 3：验证与上线

交付物：自动化测试、浏览器与视觉验证、性能报告、EdgeOne Makers 预览环境、生产部署清单。

## 17. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 产品能力、地域或 SLA 尚未确认 | 文案可能失真 | 建立事实表，未经确认不展示数字或承诺 |
| Magic UI / Aceternity 过度使用 | 性能下降、视觉同质化 | 每个动效必须服务内容，并设置性能与降级门槛 |
| 缺少真实产品截图与客户证明 | 信任感不足 | 优先制作真实控制台原型、架构图或可核验指标 |
| 中英文同步上线拖慢首版 | 延期 | 中文首发、英文架构预留，英文作为 P1 可独立验收 |
| 联系表单数据链路未确定 | 无法完成转化闭环 | 开发前确认收件、CRM、反垃圾和隐私流程 |

## 18. 审批项

以下项目确认后，才进入视觉与开发阶段：

- [x] 品牌正式中文名称：拼好机；官方域名：`pinhaoji1.cn`。
- [ ] 英文品牌名、Logo、品牌色及商标可用性。
- [ ] 首版语言：仅中文，或中文 + 英文同步上线。
- [ ] 核心客户：开发者、成长型公司、企业客户的优先级。
- [ ] 首版真实可售产品及其排序。
- [ ] 可公开的节点、带宽、客户、认证、SLA 与其他证明材料。
- [ ] 主 CTA 流程：自助注册、申请试用或联系销售。
- [ ] 控制台、文档、状态页、备案与社交账号地址。
- [ ] 联系表单接收方式：邮件、CRM 或数据库。
- [ ] 是否展示定价，以及可公开的计费信息。
- [ ] 是否允许使用 EdgeOne Makers 内置分析或其他第三方统计，并完成数据处理与跨境合规评估。
- [ ] 中国境内 ICP/公安备案、域名实名及目标海外市场法律要求的责任人和完成时间。
- [ ] 隐私政策、服务条款与目标市场的法律审核责任人。

## 19. 建议审批结论格式

审批时可直接回复以下内容：

```text
PRD v0.1：批准 / 有条件批准 / 退回修改
品牌名称：
首版语言：
主 CTA：
首版产品：
必须增加：
必须删除：
其他修改：
```
