# 门户前端环境变量
# 后端 API 地址（fetch 基准，credentials: include 携带会话 Cookie）
# 本地 dev 用 localhost:4000；生产构建必须填公网 API 域名（如 https://api.example.com，
# deploy-prod.sh 自动写入），构建期烘焙进产物，改后需重建 portal
NEXT_PUBLIC_API_URL=http://localhost:4000
