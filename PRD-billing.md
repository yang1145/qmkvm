# 拼好机云业务系统（类 WHMCS）PRD

> 文档版本：v0.1 Draft
> 文档状态：待审批，审批前不进入开发
> 关联文档：`PRD.md`（拼好机官网 Landing Page，已实现）
> 品牌与域名：拼好机 / Pinhaoji Cloud，`pinhaoji1.cn`
> 建议子系统域名：`portal.pinhaoji1.cn`（客户中心）、`api.pinhaoji1.cn`（API）、管理后台域名内网或独立子域（审批项）
> 首版语言策略：中文优先，英文随国际化需求迭代

---

## 1. 项目概述

为拼好机建设官网背后的**云业务系统**：一套面向国内用户习惯的、类 WHMCS 的在线销售与客户运营平台，覆盖「注册 → 选配下单 → 在线支付 → 自动/人工开通 → 续费升级 → 逾期处理 → 工单服务 → 开票对账」的完整生命周期。

系统由三部分组成：

1. **客户门户（Portal）**：客户自助完成购买、支付、管理服务、续费、充值、提交工单、申请开票、账户安全设置。
2. **管理后台（Admin）**：运营方管理商品、订单、服务、账务、工单、优惠、通知模板、供应模块与系统配置，基于 RBAC 分角色授权并全程审计。
3. **API 与自动化引擎（API + Worker）**：REST API 服务、支付回调处理、定时任务（续费/逾期/对账）、供应模块调度、事件与外部 Webhook。

系统与现有官网的关系：官网（本仓库 `app/` 部分）继续承担品牌与获客；官网「开始使用」按钮（`lib/site.ts` 中预留的 `links.console`）指向客户门户完成转化。两套系统同仓 Monorepo 管理，共享设计规范与部分组件，业务上单向跳转、无需复杂 SSO（官网无账户体系）。

## 2. 背景与问题

### 2.1 现状

- 官网首版已完成：首页、产品、定价（示例数据）、联系表单、法律页面，中文优先。
- 官网定价页的 `buyUrl` 为空，购买路径回退到「联系销售」，**没有任何在线交易能力**。
- 官网 `siteConfig.links.console` 已预留控制台地址字段，等待本系统填充。
- 商品形态已明确：云服务器套餐（周期计费，¥39/99/259 档位起步）、企业定制（线下签约），后续可能扩展裸金属、对象存储、CDN 等。

### 2.2 为什么自研而不用 WHMCS

| 问题 | WHMCS 现状 | 自研方案 |
| --- | --- | --- |
| 支付方式 | 无支付宝/微信官方通道，依赖第三方插件，质量参差 | 原生对接支付宝、微信支付，抽象网关层可持续扩展 |
| 登录习惯 | 邮箱+密码为主 | 手机号+短信验证码为主，符合国内习惯 |
| 中文与界面 | 翻译不全、交互陈旧 | 中文优先的原生界面，与官网视觉统一 |
| 发票 | 无中国增值税发票概念 | 内置开票申请、抬头管理、导出开票流程 |
| 数据合规 | 数据托管在自有服务器但代码闭源、历史漏洞多 | 代码开源可控、数据存储位置自主、安全可审计 |
| 定制成本 | Hook/Addon 开发受限，升级易冲突 | 事件驱动 + 模块化，供应对接自由 |
| 许可费用 | 商业 License 按月收费 | 无许可成本，投入转为研发 |

### 2.3 必须解决的问题

- 将官网「联系销售」的线下转化升级为「在线自助下单支付」的线上闭环。
- 计费正确性：金额计算、周期折算、逾期状态机、账务流水不允许出错。
- 发货自动化：支付成功后自动/半自动开通云服务器，减少人工值守。
- 安全与合规：国内支付资质、个人信息保护、操作审计、防刷防薅。

## 3. 产品目标

### 3.1 核心目标

1. 上线后 30 天内支撑完整线上交易闭环：注册 → 下单 → 支付宝/微信支付 → 开通 → 服务可管理。
2. 计费与账务零差错：所有资金操作有幂等保障、有双式账本流水、有日终对账。
3. 自动化率：自动开通成功率、续费发票自动生成率、逾期自动处理率可统计，目标首版 ≥ 90%（人工模块除外）。
4. 客户自助率：常规问题通过知识库与工单解决，客服人均并发处理能力可统计。
5. 为后续产品线（裸金属、存储、CDN）扩展提供「配置即上架」的商品模型。

### 3.2 成功指标（上线后 30 天建立基线）

- 下单到支付完成转化率可独立统计（进入结算页 → 支付成功）。
- 支付成功率（网关侧）：成功扣款笔数 / 发起支付笔数，基线后设定目标。
- 自动开通时长：支付成功 → 服务激活，P95 ≤ 5 分钟（自动模块）。
- 资金对账差异：日终对账差异笔数 = 0，差异金额 = 0。
- API 可用性 ≥ 99.9%（按月），核心接口 P95 响应 ≤ 300ms。
- 安全事件 0 起：越权访问、资金损失、数据泄露。

### 3.3 非目标（首版明确不做）

- 域名注册、续费、转移管理（P3 再评估）。
- 虚拟主机 / cPanel / Plesk 集成。
- 多租户 SaaS 化（本系统仅供拼好机自营业务使用，不对第三方输出）。
- 余额提现、退款到原路以外的渠道。
- 分销 / 代理商 / 推广返利体系（P3）。
- 手机 App（移动端以响应式 Web 覆盖，兼容微信内置浏览器）。
- 英文界面（架构预留 i18n，英文随海外业务量决定）。
- 后付费 / 合同制账期结算（企业定制单走线下，系统内仅登记）。

## 4. 用户与角色

### 4.1 客户（Portal 用户）

- **个人开发者**：1~3 台云服务器，价格敏感，重视自助开通速度与续费提醒。
- **创业团队 / 企业客户**：多台服务与服务，需要发票（增值税普票/专票）、多人协作（子账户，P2）、合同与对账单。

### 4.2 运营方（Admin 用户）

| 角色 | 职责 | 关键权限 |
| --- | --- | --- |
| 超级管理员 | 系统全部功能与配置 | 全部权限 + 权限管理 + 安全设置 |
| 财务 | 账单、交易、退款、开票 | 发票/交易/退款/开票审核，只读客户 |
| 客服 | 工单、客户沟通 | 工单全部操作，客户只读，无资金权限 |
| 技术运维 | 开通、暂停、恢复、供应模块 | 服务实例操作 + 模块配置，无资金权限 |
| 只读/审计 | 查看报表与日志 | 全局只读 |

角色为初始模板，权限点矩阵可自定义组合（见 F12.4）。

## 5. 总体架构与技术选型

### 5.1 架构总览

```text
                    ┌────────────────────────────────────────────┐
                    │              CDN / WAF（可选）              │
                    └────────────────────────────────────────────┘
        ┌───────────────┬───────────────┬───────────────┐
        │  官网 www     │ 客户门户 portal │  管理后台 admin │   （均为 Next.js）
        └───────┬───────┴───────┬───────┴───────┬───────┘
                └───────────────┼───────────────┘
                                ▼  HTTPS / JSON（REST）
                    ┌────────────────────────────┐
                    │  API 服务（Hono + Node.js） │ ← 支付回调（验签后入队）
                    │  portal / admin / open /   │
                    │  webhooks / public 分区    │
                    └─────────┬──────────┬──────┘
                              ▼          ▼
                    ┌──────────────┐  ┌──────────────────┐
                    │ MySQL 8 主库 │  │ Redis（会话/缓存/ │
                    │ （InnoDB）   │  │ 限流/BullMQ 队列）│
                    └──────────────┘  └────────┬─────────┘
                              ▲                ▼
                    ┌─────────┴──────────────────────────┐
                    │  Worker（BullMQ 消费者 + 定时任务）  │
                    │  供应模块 / 通知 / 对账 / 自动化      │
                    └─────────┬──────────────────────────┘
                              ▼
        ┌──────────┬──────────┬──────────┬──────────┬──────────┐
        │ 支付宝   │ 微信支付  │ 聚合通道  │ SMS/邮件  │ 供应面板  │
        │（官方SDK）│（官方V3） │（可选）   │ 服务商    │ HTTP API │
        └──────────┴──────────┴──────────┴──────────┴──────────┘
```

### 5.2 Monorepo 布局

本仓库（pinhaoji-web）演进为 pnpm workspaces + Turborepo 的 Monorepo，现有官网迁入 `apps/www`，保持线上行为不变：

```text
pinhaoji-web/
├─ apps/
│  ├─ www/          # 现有官网（迁移，改动最小化）
│  ├─ portal/       # 客户门户（Next.js）
│  ├─ admin/        # 管理后台（Next.js）
│  ├─ api/          # Hono API 服务（Node.js runtime）
│  └─ worker/       # BullMQ Worker + 定时任务（可与 api 同镜像、不同入口）
├─ packages/
│  ├─ contracts/    # Zod Schema + OpenAPI 类型定义（前后端共享）
│  ├─ db/           # Drizzle Schema、迁移、种子数据
│  ├─ core/         # 领域服务：订单/计费/账单/账本/优惠/服务生命周期
│  ├─ auth/         # 会话、密码、短信验证码、TOTP、RBAC
│  ├─ payments/     # 支付网关适配器（支付宝/微信/聚合/余额）
│  ├─ provisioning/ # 供应模块 SDK + 内置模块（manual/http-api/demo）
│  ├─ notifications/# 邮件/短信/站内信 模板与发送
│  ├─ ui/           # 共享 shadcn/ui 组件与 Design Tokens（三端复用）
│  ├─ logger/       # pino 结构化日志封装
│  └─ config/       # 共享 tsconfig / eslint / prettier
├─ docker/          # Compose、Dockerfile、Nginx/Caddy 配置
├─ pnpm-workspace.yaml
└─ turbo.json
```

### 5.3 技术选型与理由

| 层 | 选型 | 理由（市面主流度 × 项目适配） |
| --- | --- | --- |
| 语言 | TypeScript（严格模式） | 全栈类型安全，与官网一致 |
| Monorepo | pnpm workspaces + Turborepo | 当前 Node 生态最主流组合，任务编排与缓存成熟 |
| API 框架 | Hono（`@hono/node-server`，Node.js runtime） | 用户指定；轻量高性能、中间件生态好、TS 原生、OpenAPI 支持完善 |
| 校验 | Zod | 校验事实标准；Schema 放 `packages/contracts` 前后端复用 |
| ORM | Drizzle ORM + drizzle-kit | Hono 生态最流行 ORM；SQL 透明可控、迁移可靠，适合计费类精确查询 |
| 数据库 | MySQL 8.x（InnoDB，utf8mb4） | 用户指定；事务与行级锁保障账务一致性 |
| 缓存/队列 | Redis 7 + BullMQ | Node 生态最主流任务队列；重试、退避、延迟任务、重复调度齐备 |
| 前端 | Next.js（App Router）+ React + Tailwind CSS + shadcn/ui | 与官网同栈，`packages/ui` 直接复用，学习与维护成本最低 |
| 表单 | React Hook Form + `@hookform/resolvers` | 与官网一致 |
| 密码哈希 | argon2id | 当前密码存储最佳实践 |
| 认证 | 自研轻量会话层（HttpOnly Cookie + 服务端 Session） | 主登录方式为手机号+短信验证码，且 portal/admin 分离、需强制审计；`better-auth` 等成品库作为备选评估项 |
| 支付 | 支付宝官方 `alipay-sdk`、微信支付官方 API v3 | 资金安全第一原则；聚合通道仅作为可选适配器 |
| 短信 | 阿里云短信 / 腾讯云短信（适配器抽象） | 国内主流，签名模板审核流程明确 |
| 邮件 | Nodemailer + SMTP（阿里云邮件推送等） | 简单可靠，可替换 |
| 人机验证 | 腾讯云验证码 / 极验 / Cloudflare Turnstile（适配器抽象） | 国内可用性与成本平衡，审批定 default |
| 日志 | pino（JSON 结构化） | Node 主流，便于采集与检索 |
| 测试 | Vitest（单测）+ Playwright（E2E）+ MSW（网关 Mock） | 主流组合；计费逻辑要求高覆盖 |
| 代码质量 | ESLint（flat config）+ Prettier + TypeScript | 与官网一致 |
| 部署 | Docker Compose（初期单机）→ 可平滑迁 K8s | 起步简单，演进路径清晰 |
| 监控告警 | `/healthz`、`/metrics`（可选 Prometheus）+ Sentry + 钉钉/飞书/企微 Webhook 告警 | 运维成本与时效平衡 |

依赖版本在项目初始化当日锁定最新稳定、互相兼容的版本，不使用 canary、beta 或 experimental 版本作为生产基线。`apps/www` 迁移时遵循仓库 `AGENTS.md` 对 Next.js 版本差异的约束。

### 5.4 关键技术决策

1. **金额表示**：全链路使用 `BIGINT` 存储最小货币单位（分），展示层再格式化；禁止 float 参与任何计算。
2. **幂等**：支付回调以「网关事件唯一 ID + 处理状态表」幂等；开放 API 支持 `Idempotency-Key`（24 小时去重）；供应任务以任务 ID + 状态机保证不重复开通。
3. **事务边界**：资金相关操作（扣余额、标记账单已付、生成交易流水）在单事务内完成，流水表带 `balance_after` 快照，日终对账校验。
4. **前后端契约**：所有接口用 `@hono/zod-openapi` 定义，类型自动生成给 `portal`/`admin` 前端，消除手写接口类型漂移。
5. **多币种**：首版仅 CNY 结算；`currencies` 与 `exchange_rate` 表结构预留，美元展示（P2）、美元结算（P3）。
6. **运行时**：Hono 跑在 Node.js 长驻进程（MySQL 连接池、长任务、Cron 需要），不使用 Edge Runtime。

## 6. 核心领域模型与状态机

### 6.1 核心表（按域分组，详细列以迁移文件为准）

| 域 | 表 | 说明 |
| --- | --- | --- |
| 账户 | `users`、`user_identities`（email/phone 多身份）、`sessions`、`sms_codes`、`user_profiles`（实名）、`password_reset_tokens` | 一用户多登录身份 |
| 商品 | `product_groups`、`products`、`product_pricing`（按周期：单价/setup费/续费价）、`configurable_groups`、`configurable_options`、`option_pricing`、`product_addons`、`addon_pricing` | 支持可配置选项（CPU/内存/带宽/流量/地域） |
| 订单 | `orders`、`order_items` | 购物车结算产物 |
| 服务 | `services`（实例）、`service_options`、`service_addons`、`provision_tasks` | WHMCS 中的 hosting 服务实例 |
| 账务 | `invoices`、`invoice_items`、`transactions`（网关流水）、`credit_ledger`（余额双式账本）、`refunds` | 金额全部为分 |
| 支付 | `payment_intents`（支付单）、`gateway_events`（回调幂等）、`gateways`（网关配置） | 支付单关联账单 |
| 发票 | `invoice_titles`（开票抬头）、`fapiao_requests`（开票申请） | 中国增值税发票 |
| 营销 | `promotions`、`promotion_usages` | 优惠码 |
| 支持 | `ticket_departments`、`tickets`、`ticket_replies`、`attachments`、`kb_categories`、`kb_articles` | 工单 + 知识库 |
| 通知 | `notification_templates`（邮件/短信）、`notification_logs`、`user_notifications`（站内信） | 模板变量渲染 |
| 管理 | `admin_users`、`admin_roles`、`admin_role_permissions`、`audit_logs` | RBAC + 审计 |
| 系统 | `settings`、`currencies`、`exchange_rates`、`api_tokens`、`webhook_endpoints`、`webhook_deliveries`、`job_runs`（定时任务执行记录）、`outbox_events` | 配置与开放能力 |

### 6.2 关键状态机

**订单 order**：`pending(待支付) → paid(已支付) → processing(开通中) → completed(完成)`；分支 `cancelled(已取消，未支付或管理员)`、`failed(开通失败，转人工)`。

**服务 service**（对齐 WHMCS 语义）：

```text
pending(待开通) ──开通成功──▶ active(正常)
active ──到期未续费 D+N──▶ suspended_overdue(逾期暂停)
active ──管理员操作──▶ suspended_manual(人工暂停)
suspended_* ──续费支付/恢复──▶ active
suspended_overdue ──到期 D+M──▶ terminated(已释放)
active/pending ──用户申请取消+到期──▶ cancelled(已取消，不释放)
active ──升降配──▶ active（带 upgrade 记录，不变状态）
```

**账单 invoice**：`draft → unpaid → paid`；分支 `partially_refunded`、`refunded`、`void(作废/红冲)`。

**支付单 payment_intent**：`created → paying(已拉起) → success / failed / expired(超时关闭) / canceled`。

**工单 ticket**：`open(待受理) → answered(已回复待客户) → customer_reply(客户已回复) → in_progress(处理中) → resolved(已解决) → closed(已关闭)`；客户在任何非关闭状态回复自动回到 `customer_reply`。

### 6.3 计费周期与规则

- 支持周期：一次性（onetime）、月付、季付、半年付、年付、两年付、三年付；周期与价格在 `product_pricing` 配置。
- 计费模式：**预付费**——先支付后开通；续费账单可配置提前 N 天自动生成（默认 3 天）。
- 到期时间计算以 `next_due_date` 为基准，支付成功后 `next_due_date += 周期`，跨月按自然日累加（与 WHMCS 一致），可配置「按开户日对齐」。
- 升降配折算（prorata）：剩余周期价值按天比例折算为余额抵扣新配置价格，差额生成升级账单；降配仅折算不退款出金（差额入余额）。
- 逾期处理（全局可配置，默认值审批确定）：T-14/7/3/1 天提醒 → 到期日生成续费账单（余额充足可自动支付）→ D+1/3/5 逾期提醒 → D+3 逾期暂停 → D+15 终止并释放资源。
- 税费：价格默认含税展示（国内习惯），可配置价税分离与税率（默认 0，审批确定）。

## 7. 功能需求

优先级定义：P0 = 首版必须；P1 = 首版后第一个迭代；P2 = 规划中；P3 = 远期。

### F1 账户与认证（Portal）

**描述**：客户注册、登录、找回凭证、会话与账户安全。

| 需求 | 优先级 | 实现要点 |
| --- | --- | --- |
| 手机号 + 短信验证码注册/登录（主流程，注册登录合一） | P0 | 验证码 6 位、5 分钟有效、1 分钟/条、5 条/天/号；验证码哈希存储于 Redis；注册即建 `users` + `user_identities(phone)` |
| 邮箱 + 密码注册/登录 | P0 | argon2id 哈希；密码规则：≥8 位含字母数字；注册发欢迎邮件 |
| 找回密码 / 更换绑定手机 | P0 | 旧凭证验证 + 新验证码双因子；换绑冷却 24 小时 |
| 会话管理 | P0 | HttpOnly + Secure + SameSite=Lax Cookie；服务端 Session（Redis，TTL 30 天滑动）；登录/改密后轮换 Session ID；「退出全部设备」 |
| 登录失败保护 | P0 | IP + 账号双维度限流（5 次/15 分钟锁定），图形/行为验证码升级触发 |
| 双因素认证（TOTP） | P1 | 客户可选开启；恢复码一次性 |
| 微信扫码登录 | P2 | 需微信开放平台资质，`user_identities(wechat)` 预留 |
| 子账户/联系人（企业多人协作） | P2 | 主账户授权子账户操作服务与工单，资金操作仅主账户 |

**注册合规前置**：注册页呈现《服务协议》《隐私政策》勾选（链接指向官网法律页或本系统同源页面）。

### F2 实名认证与合规（Portal）

**描述**：按销售区域法规决定是否强制实名，系统侧预留能力。

| 需求 | 优先级 | 实现要点 |
| --- | --- | --- |
| 实名信息登记（个人：姓名+身份证号；企业：企业名称+统一社会信用代码） | P1 | 证件号 AES-256-GCM 加密存储，脱敏展示；表单与列表不可明文导出 |
| 实名核验（对接阿里云/腾讯云人证核验） | P2 | `packages/core` 预留 `IdentityVerifier` 适配器接口；是否强制由设置项控制 |
| 未实名拦截策略 | P1 | 可配置：不限制 / 下单拦截 / 仅特定商品拦截 |

### F3 商品与定价（Admin 配置，Portal 展示）

**描述**：运营方配置商品目录，客户按周期与选配项购买。

| 需求 | 优先级 | 实现要点 |
| --- | --- | --- |
| 商品分组与商品管理 | P0 | 分组排序、商品上下架、商品描述富文本、与官网定价页联动的数据来源（官网 `pricing.json` 后续可改为拉取本系统公开 API，P2） |
| 周期定价 | P0 | 每商品 × 每周期：首次价、续费价、开通费（setup fee，可为 0）；支持「首期优惠价」 |
| 可配置选项组 | P0 | 选项类型：下拉/单选/多选/数量；每选项按周期差价计费（例：+2GB 内存 +¥10/月）；选项间支持联动显示（P1） |
| 附加服务（Addon） | P1 | 独立计费的增值项（如快照备份、独立 IP），可随服务订购或后补 |
| 库存控制 | P0 | 商品级库存数量，售罄下架/显示「缺货」；支持不限量 |
| 隐藏商品（仅链接可达） | P1 | 用于渠道定向与老客户续费 |
| 商品升降配规则 | P0 | 配置允许升级/降级的目标商品与折算规则 |

### F4 购物车与订单（Portal）

**描述**：选配 → 结算 → 支付的下单闭环。

**主流程（实现方案）**：

1. 商品页选周期 + 可配置选项 → 加入购物车（服务端购物车，`carts` Redis 结构或表，登录后合并）。
2. 结算页：核对配置、输入优惠码（实时验算）、选择支付方式、勾选协议。
3. 提交结算：服务端以「当前商品快照价格 + 选项价格」重算金额（**不信任前端价格**），生成 `order + order_items` 与关联 `invoice(unpaid)` 与 `payment_intent`，返回支付参数。
4. 支付成功回调 → 账单标记已付 → 生成开通任务 → 服务开通 → 通知客户。

| 需求 | 优先级 | 实现要点 |
| --- | --- | --- |
| 购物车（多商品、改配置、移除） | P0 | 服务端为准；未登录可浏览，结算前必须登录 |
| 优惠码验算与应用 | P0 | 规则见 F11；结算页实时显示折后价 |
| 余额抵扣 | P0 | 结算时可用余额全额抵扣；余额不足部分在线支付（组合支付：余额优先抵扣 + 网关收差额，P1） |
| 订单列表与详情 | P0 | 状态、金额、账单、关联服务一览 |
| 未支付订单管理 | P0 | 支付单 15~30 分钟未支付自动关闭（可配置）；订单可主动取消 |
| 续费下单（单服务/批量） | P0 | 从服务详情一键生成续费账单；批量续费合并一张账单 |
| 升级/降级下单 | P0 | prorata 折算预览（展示抵扣与差额）→ 确认后生成升级账单 → 支付后调供应模块变更 |
| 库存扣减 | P0 | 下单锁定（支付成功转实扣），关单释放；`SELECT ... FOR UPDATE` 防超卖 |

### F5 支付与充值（Portal + 网关）

**描述**：资金入口，安全等级最高。

| 需求 | 优先级 | 实现要点 |
| --- | --- | --- |
| 支付网关抽象层 | P0 | `interface PaymentGateway { createPayment(), handleCallback(), query(), refund() }`；新增通道只加适配器 |
| 支付宝：当面付（扫码）+ 电脑网站支付 | P0 | 官方 SDK，RSA2 验签；异步通知幂等处理 |
| 微信支付：Native（扫码）+ H5/JSAPI | P0 | 官方 API v3，平台证书验签、回调解密 |
| 余额充值 | P0 | 生成充值账单（标记 type=credit，不可开票为消费）→ 支付成功入 `credit_ledger`；充值金额按档或自定义 |
| 余额支付 | P0 | 单事务：校验余额 → 扣减写流水 → 标记账单已付 |
| 退款 | P1 | 原路退回；管理后台发起，需财务角色 + 二次确认；部分退款支持 |
| 聚合通道（虎皮椒/易支付类） | P1（可选） | 作为适配器实现；接入前须法务评估通道资质与二清风险（审批项） |
| Stripe / PayPal（国际卡与外币） | P2 | 面向海外客户；`gateways` 配置启用 |
| USDT/加密支付 | P3 | 合规评估后决定 |
| 自动续费 | P2 | 余额自动扣款（P1，见 F7）；支付宝周期扣款协议/微信委托代扣（P2，需资质） |
| 对账 | P0 | 每日拉取网关账单与本地 `transactions` 比对，差异告警 + 差异处理工作台 |

**回调处理规范**：所有网关回调先验签 → 写 `gateway_events`（唯一事件 ID，幂等）→ 入队异步处理（快速返回成功）→ Worker 在事务内完成「流水落库 + 账单状态 + 后续动作」；处理失败自动重试并告警。

### F6 账单与发票（Portal + Admin）

**描述**：账单（invoice）是催收与对账依据；发票（fapiao）是中国税务合规动作，两者分离。

| 需求 | 优先级 | 实现要点 |
| --- | --- | --- |
| 账单生成与管理 | P0 | 账单编号规则可配置（如 `PHJ-YYYYMM-XXXXXX`）；账单项明细（商品、周期、折扣行）；后台可手工开账单（人工定制单登记） |
| 账单支付 | P0 | 未付账单可随时支付（余额/在线）；支付成功发邮件+站内信 |
| 账单 PDF | P1 | 服务端渲染 PDF（如 `@react-pdf/renderer` 或 HTML→PDF 服务），中文嵌入字体 |
| 逾期标记与催收记录 | P0 | 逾期天数展示；提醒发送记录留痕 |
| 作废/红冲 | P0 | 仅财务角色；作废需关联原因；已支付账单红冲生成负向流水 |
| 开票抬头管理 | P1 | 个人/企业抬头 CRUD；企业抬头含税号、注册地址、电话、开户行 |
| 开票申请 | P1 | 客户按「已支付未开票金额」申请；类型：增值税电子普票/专票；状态：待审核/已开出/已驳回；驳回须原因 |
| 开票处理 | P1 | 后台审核 + 导出开票清单（CSV），线下电子税务局或税务服务商开票后回填发票号与 PDF；对接百望/诺诺等开票 API（P2） |

### F7 服务生命周期与自动化（Worker）

**描述**：WHMCS 的核心价值——按时自动执行续费、提醒、暂停、终止。

| 需求 | 优先级 | 实现要点 |
| --- | --- | --- |
| 续费账单自动生成 | P0 | 每日定时扫描 `next_due_date` 到期前 N 天的服务，生成续费账单并通知 |
| 到期提醒 | P0 | T-14/7/3/1（可配置）短信+邮件+站内信 |
| 余额自动续费 | P1 | 到期日服务余额充足则自动扣款续费；客户可按服务开关（默认关闭，opt-in）；扣款失败回退人工流程 |
| 逾期提醒与暂停 | P0 | D+1/3/5 提醒；D+N（默认 3）自动暂停：调供应模块 suspend，状态 `suspended_overdue`，通知客户 |
| 逾期终止 | P0 | D+M（默认 15）自动终止：调 terminate，数据释放策略由供应模块定义；终止前强提醒（审批确定默认值） |
| 任务重试与死信 | P0 | 供应任务指数退避重试（最多 5 次）；失败入死信队列并告警，后台人工处理工作台 |
| 手动操作 | P0 | 后台对服务手动开通/暂停/恢复/终止/改配，均产生任务与审计记录 |
| 用户申请取消 | P0 | 立即取消（到期终止，可配退款策略）或到期取消；取消申请后台可见可撤回（终止前） |
| 自动化执行记录 | P0 | `job_runs` 记录每次定时任务结果（扫描数/处理数/失败数），后台可视化 |

### F8 供应模块（Provisioning，开放扩展点）

**描述**：WHMCS Module 的等价物——支付成功后如何交付资源。这是系统与资源面板（拼好机自有云面板/第三方面板）的边界。

**模块接口（SDK 形态）**：

```ts
interface ProvisionModule {
  meta: { code: string; name: string; version: string }
  validateConfig(config: Json): Promise<ValidationResult>       // 模块参数校验
  provision(ctx: Ctx, service: ServiceInput): Promise<ModuleResult>   // 开通
  suspend(ctx, service): Promise<ModuleResult>                  // 暂停
  unsuspend(ctx, service): Promise<ModuleResult>                // 恢复
  terminate(ctx, service): Promise<ModuleResult>                // 终止/释放
  changePackage(ctx, service, from, to): Promise<ModuleResult>  // 升降配
  syncStatus(ctx, service): Promise<RemoteSync>                 // 状态回查
  testConnection(config): Promise<TestResult>                   // 连通性测试
}
```

| 需求 | 优先级 | 实现要点 |
| --- | --- | --- |
| `manual` 人工模块 | P0 | 支付后生成待办任务，后台人工完成并回填交付信息（IP、账号交付方式）；适合首版无对接时冷启动 |
| `http-api` 通用 HTTP 模块 | P0 | 配置化对接自研/第三方面板：开通/暂停/恢复/终止分别映射 HTTP 调用（URL、方法、鉴权、字段映射、签名）；HMAC 签名支持；响应校验规则 |
| `demo` 演示模块 | P0 | 立即成功，用于测试环境全链路验证 |
| 专属模块（对接拼好机云面板） | P1 | 按 `http-api` 或定制模块实现，接口文档由资源侧提供（依赖项，见风险） |
| 交付信息展示 | P0 | 服务详情页展示模块产出的自定义字段（如 IP、登录入口、初始凭据脱敏） |
| 供应任务工作台 | P0 | 后台查看任务队列、重试、跳过、人工完成；失败原因展示 |
| 状态回查同步 | P1 | 定时 `syncStatus` 校正本地状态与远端差异 |

**任务执行规范**：所有模块调用在 Worker 内执行，超时控制（默认 120s）、结果落库（请求/响应脱敏存储）、失败自动重试、每步审计。

### F9 工单与帮助中心（Portal + Admin）

| 需求 | 优先级 | 实现要点 |
| --- | --- | --- |
| 工单部门（售前/技术/财务/投诉） | P0 | 部门路由与接收邮箱配置 |
| 提交工单 | P0 | 部门、主题、优先级（低/中/高/紧急）、内容富文本、附件（类型与大小白名单，隔离存储、病毒扫描 P2） |
| 工单回复与状态流转 | P0 | 客户/客服双向回复；状态机见 6.2；客服回复后自动邮件通知客户 |
| 报修关联服务 | P0 | 提交时选择关联服务实例，客服可见服务信息（脱敏） |
| SLA 与超时提醒 | P1 | 按优先级设定首次响应与解决时限，超时升级提醒（可配置） |
| 工单转内部备注 | P0 | 客户不可见的内部备注（WHMCS Inner Note 等价） |
| 知识库 | P1 | 分类、文章、富文本、公开/登录可见、搜索；工单回复可引用知识库快速回复 |
| 评价 | P2 | 工单关闭后满意度评分 |

### F10 通知中心

| 需求 | 优先级 | 实现要点 |
| --- | --- | --- |
| 邮件通知 | P0 | 模板化（HTML+文本），变量渲染 `{{user.name}}` 等；关键事件：注册、订单支付成功、开通成功、账单生成、支付提醒、到期提醒、逾期暂停/终止、工单回复、退款、充值到账 |
| 短信通知 | P0 | 国内通道；限交易与提醒类（验证码、支付成功、到期/逾期提醒、工单回复）；营销短信不做 |
| 站内通知 | P0 | 通知中心列表、未读角标、已读/全部已读 |
| 模板管理 | P1 | 后台可视化编辑模板、变量说明、测试发送、启用开关 |
| 运营告警（面向管理员） | P0 | 钉钉/飞书/企微 Webhook：开通失败、对账差异、死信队列、余额异常、系统错误 |
| 通知偏好 | P1 | 客户可按渠道（邮件/短信）与类别设置接收偏好（交易类不可关闭） |
| 发送失败处理 | P0 | 重试退避；失败记录可查询；短信余额不足提前告警 |

### F11 优惠与营销

| 需求 | 优先级 | 实现要点 |
| --- | --- | --- |
| 优惠码 | P0 | 类型：百分比 / 固定金额 / 首期特价；范围：全场 / 指定商品 / 指定分组 / 指定周期；限制：有效期、总次数、每用户次数、最低消费、仅新客户（首单）；叠加规则：默认互斥 |
| 优惠码使用统计 | P0 | 使用次数、带来的订单金额（后台报表） |
| 首期促销价 | P0 | 商品定价层面直接配置（区别于优惠码） |
| 充值赠送 | P2 | 阶梯赠送（充 100 送 10）；赠送金独立标记、不可提现、消费优先级规则 |
| 推荐返利 | P3 | 非目标，远期评估 |

### F12 管理后台（Admin）

| 需求 | 优先级 | 实现要点 |
| --- | --- | --- |
| 仪表盘 | P0 | 今日/本月：新注册、订单数、GMV、支付成功率、待处理任务、逾期服务、待回复工单、7/30 天趋势图 |
| 客户管理 | P0 | 搜索（手机号/邮箱/ID/名称）、详情（资料、服务、订单、账单、余额、工单、登录记录）、手工调整余额（双人复核：发起+审批，P1）、登录客户视图（管理员以只读方式查看门户，全程审计） |
| 订单/服务管理 | P0 | 搜索、筛选、手工操作（见 F7/F8） |
| 账务工作台 | P0 | 账单/交易/退款/充值列表与详情；对账差异处理 |
| 商品/优惠管理 | P0 | 见 F3/F11 |
| 工单工作台 | P0 | 见 F9 |
| RBAC 与管理员管理 | P0 | 角色模板 + 自定义权限点矩阵；管理员强制 2FA（P1）；后台独立域名 + 可选 IP 白名单（部署层） |
| 审计日志 | P0 | 记录：后台登录（成功/失败）、所有写操作（谁/何时/改了什么，记录前后值摘要）、资金操作、数据导出、权限变更；append-only，仅超管可读，保留 ≥ 180 天 |
| 系统设置 | P0 | 站点信息、订单/计费参数（提前生成天数、逾期阈值、支付单超时）、税务参数、通知渠道密钥（加密存储、后台不可回显明文）、合规开关（实名策略）、维护模式 |
| 数据导出 | P1 | 客户/订单/账单/开票清单 CSV 导出（需权限 + 审计；敏感字段默认脱敏） |
| 报表 | P1 | 收入（日/月/商品维度）、流失（退订/终止）、续费率、工单量；图表展示 |
| 操作日志检索与告警规则 | P2 | 按人/类型/时间检索；异常行为（连续失败登录、批量导出）实时告警 |

### F13 开放 API 与事件（P1~P2）

| 需求 | 优先级 | 实现要点 |
| --- | --- | --- |
| 管理 API（Open API） | P1 | Token 认证（`api_tokens`：仅显示一次、前缀可识别、支持失效）；能力：客户/订单/服务/账单查询与服务操作；`Idempotency-Key` 支持；速率限制按 Token 配额 |
| OpenAPI 文档 | P1 | 由代码生成（`@hono/zod-openapi`），内部文档站托管 |
| 领域事件外发 Webhook | P2 | 事件：`order.paid`、`service.activated`、`service.suspended`、`invoice.paid`、`refund.created`、`ticket.created` 等；HMAC-SHA256 签名 + 时间戳防重放；指数退避重投，投递记录可查 |
| 插件化扩展 | P2 | 事件总线（进程内 + 队列）开放订阅注册接口，第三方逻辑以包形式挂载，不侵入核心 |

### F14 安全与风控（横切）

| 需求 | 优先级 | 实现要点 |
| --- | --- | --- |
| 传输安全 | P0 | 全站 HTTPS（TLS 1.2+）、HSTS；后台与 API 独立证书 |
| 注入与 XSS | P0 | Drizzle 参数化查询杜绝 SQL 注入；富文本入库前服务端白名单过滤（如 sanitize-html）；CSP 响应头 |
| CSRF | P0 | SameSite Cookie + 写操作双重提交 Token 校验 |
| 越权防护 | P0 | 所有资源查询强制 scope by `user_id`（对象级授权中间件统一处理）；后台按权限点鉴权 |
| 限流防刷 | P0 | Redis 滑动窗口：注册/登录/验证码/结算/回调全局 IP 限流；人机验证码在注册与异常登录时触发 |
| 资金安全 | P0 | 金额服务端计算；回调验签 + 幂等；金额一致性校验（回调金额与账单不符即告警挂起人工处理） |
| 敏感数据 | P0 | 手机号/邮箱脱敏展示；身份证/税号加密存储；日志脱敏规范（手机号、密码、Token、验证码禁入日志） |
| 密钥管理 | P0 | 环境变量注入（不进仓库）；网关密钥加密落库；密钥轮换方案文档化 |
| 备份与恢复 | P0 | 见 R4 运维 |
| 渗透测试与漏洞扫描 | P1 | 上线前一轮第三方或内部渗透测试；依赖漏洞扫描进 CI |

## 8. 页面地图

### 8.1 客户门户（portal）

| 路由 | 页面 | 优先级 |
| --- | --- | --- |
| `/login`、`/register`、`/forgot` | 登录（验证码/密码双 Tab）、注册、找回 | P0 |
| `/` | 工作台：服务概览、待办（未付账单/即将到期）、余额、通知 | P0 |
| `/products` | 商品列表（按分组） | P0 |
| `/products/:slug` | 商品配置（周期+选项+价格实时计算） | P0 |
| `/cart`、`/checkout` | 购物车、结算（优惠码/支付方式） | P0 |
| `/pay/:invoiceId` | 收银台：扫码/跳转引导、支付状态轮询 | P0 |
| `/services`、`/services/:id` | 服务列表、详情（交付信息、续费、升级、取消） | P0 |
| `/orders` | 订单列表与详情 | P0 |
| `/invoices`、`/invoices/:id` | 账单列表、详情（支付/下载 PDF） | P0 |
| `/credits` | 余额与流水、充值入口 | P0 |
| `/fapiao` | 开票申请与抬头管理、开票记录 | P1 |
| `/tickets`、`/tickets/:id`、`/tickets/new` | 工单列表、会话、创建 | P0 |
| `/kb`、`/kb/:slug` | 知识库（P1） | P1 |
| `/notifications` | 站内通知 | P0 |
| `/account`、`/account/security`、`/account/identity` | 资料、安全（密码/2FA/会话）、实名 | P0/P1 |

### 8.2 管理后台（admin）

仪表盘、客户、订单、服务、供应任务、账单、交易与退款、对账、开票申请、商品与定价、可配置选项、附加服务、优惠码、工单、知识库、部门、通知模板、事件与 Webhook、自动化任务、API Token、管理员与角色、审计日志、系统设置。

## 9. 接口规范

- **风格**：REST + JSON；路径分区 `/api/v1/portal|admin|open|public|webhooks/*`。
- **认证**：portal/admin 用 Session Cookie；open 用 `Authorization: Bearer phjt_...` Token；webhooks 按网关签名规则验签，无 Session。
- **错误结构**：

```json
{ "code": "PAYMENT_AMOUNT_MISMATCH", "message": "回调金额与账单不一致，已转人工处理", "requestId": "req_xxx", "details": {} }
```

HTTP 状态码语义化使用；业务错误码按域分段（`AUTH_*`、`ORDER_*`、`PAYMENT_*`、`BILLING_*`、`TICKET_*`、`PERM_*`），全量清单随代码生成文档。所有响应含 `requestId` 便于排障。

- **分页**：`?page=1&pageSize=20`，响应 `{ items, total, page, pageSize }`；列表接口统一筛选参数规范。
- **时间**：API 传输 ISO 8601 UTC，前端本地化显示；金额传输整数（分）+ `currency` 字段。

## 10. 定时任务清单（Worker）

| 任务 | 频率 | 逻辑摘要 |
| --- | --- | --- |
| 续费账单生成 | 每日 03:00 | 到期前 N 天服务生成账单 + 通知 |
| 到期/逾期提醒 | 每日 09:00 | T-14/7/3/1、D+1/3/5 分批发送 |
| 余额自动续费 | 每日 00:30 | 余额充足的服务自动扣款续费 |
| 逾期暂停 | 每日 04:00 | D+N 调供应模块暂停 |
| 逾期终止 | 每日 04:30 | D+M 调供应模块终止 |
| 支付单超时关闭 | 每分钟 | 超 15~30 分钟未支付关单（释放库存） |
| 网关对账 | 每小时增量 + 每日 06:00 全量 | 拉单比对，差异入处理队列并告警 |
| 供应任务重试/死信扫描 | 持续（队列） | 指数退避；死信告警 |
| Webhook 重投 | 持续（队列） | 最多 N 次（默认 8 次）指数退避 |
| 状态回查同步 | 每日 05:00 | `syncStatus` 校正差异 |
| 会话/验证码/过期数据清理 | 每周 | Redis 与表清理 |
| 数据库备份 | 每日 02:00 | 见 R4 |
| job_runs 汇总健康检查 | 每日 07:00 | 昨日任务成功率报表 + 异常告警 |

任务基于 BullMQ Repeatable（分布式锁防多跑），单次执行有超时与手动「立即执行」（后台触发，审计留痕）。

## 11. 关键业务流程（实现方案）

### 11.1 下单支付开通（主链路）

```text
商品配置 → 加入购物车 → 结算（优惠码/余额）→ 服务端重算金额
  → 事务：创建 order + invoice(unpaid) + payment_intent，锁库存
  → 返回支付参数（支付宝/微信/余额即时完成）
  → 【余额】事务：扣余额+流水+invoice=payment_intent=success → 直接触发开通
  → 【在线】用户支付 → 网关回调（验签+幂等落库）→ 事务：transaction+invoice paid
  → 发布 order.paid 事件（BullMQ）
  → Worker：生成 provision_task(provision) → 调供应模块
  → 成功：service=active，next_due_date 设置，发开通通知，order=completed
  → 失败：重试→死信→告警→后台人工工作台，order=processing(转人工标记)
```

### 11.2 续费（含余额自动续费）

手动：服务详情/账单页 → 生成续费账单 → 支付 → 到期日顺延。
自动：到期日定时任务扫描 → 余额充足且用户已开启自动续费 → 扣款 → 顺延 + 通知；余额不足 → 通知提醒，进入逾期流程。

### 11.3 逾期处理状态机

账单逾期（`next_due_date` 已过且未续费）→ D+1/3/5 提醒 → D+3 暂停（suspend）→ D+15 终止（terminate，释放资源前最终提醒）→ 全程通知可追溯，管理员可随时手工干预或豁免。

### 11.4 退款

财务发起（全额/部分）→ 二次确认 → 网关原路退回 → 回调确认 → `refunds` 落库 + 负向账务流水 + 关联账单状态更新（`refunded/partially_refunded`）→ 服务处置策略选择（保持/暂停/终止）→ 通知客户。

### 11.5 对账

每小时增量拉取网关成功订单 ↔ 本地 `transactions`；差异类型：有网关无本地（掉单 → 自动补单或告警人工）、有本地无网关（可疑流水冻结核查）、金额不一致（冻结 + 告警）。每日全量对账邮件报表给财务角色。

## 12. 合规与法律（国内）

1. **主体与资质**：支付宝/微信商户号需大陆主体与对应经营范围；若向中国大陆公众提供服务，评估 ICP 备案/公安联网备案要求；域名实名。责任人与时间见审批项。
2. **个人信息保护（PIPL）**：隐私政策与用户协议更新（覆盖账户、交易、实名、日志数据的处理目的/范围/期限）；最小必要采集；敏感字段加密；用户提供账号数据导出与注销渠道（注销：结清账单 → 服务到期处理 → 软删除 + 敏感字段抹除，保留法定交易记录）。
3. **实名与网络安全**：销售中国大陆节点云服务器需实名（本系统预留开关）；日志留存（网络安全法要求 ≥ 6 个月）。
4. **发票税务**：开票主体与税控方式（电子税务局手工 / 税务服务商 API）由财务确认；系统不代开发票，仅管理与申请流程。
5. **营销合规**：优惠码/促销表述遵守《广告法》《反不正当竞争法》，不虚构原价与限时。

## 13. 性能与可用性

- **性能预算**：公开商品接口 P95 ≤ 200ms；门户核心接口 P95 ≤ 300ms；后台列表 P95 ≤ 800ms；收银台拉起支付 ≤ 2s。
- **容量基线**：首版按 1 万注册用户、1000 活跃服务、日均 500 订单设计，单机 Compose 可承载；超过 10 倍时评估读写分离与水平扩展。
- **可用性**：API 单实例可用性 ≥ 99.9%/月；部署采用 api 双副本 + 负载均衡（Compose 即可）；MySQL 主从（P2）与每日备份恢复演练（P1 每季一次）。
- **降级**：支付回调与开通链路不依赖任何非核心服务（仅 MySQL/Redis）；短信通道故障自动降级邮件；维护模式页可配置。

## 14. 测试与质量

| 层 | 范围 | 要求 |
| --- | --- | --- |
| 单元测试（Vitest） | `packages/core` 计费引擎：周期计算、prorata 折算、优惠码、余额账本、逾期状态机、金额格式化 | 行覆盖 ≥ 90%，边界用例（月末/闰年/部分退款/叠加折扣禁止）全覆盖 |
| 集成测试 | API + MySQL（Testcontainers）+ Redis | 关键接口契约、权限矩阵（每角色 × 关键端点）、限流 |
| 网关测试 | MSW Mock 支付宝/微信回调（签名正确/错误/重复/金额不符） | 回调幂等与异常路径全覆盖 |
| E2E（Playwright） | 注册 → 下单 → 支付（mock 网关）→ 开通（demo 模块）→ 续费 → 逾期暂停 → 退款 → 工单 全链路 | CI 每次发布前跑通；后台关键路径覆盖 |
| 对账测试 | 构造掉单/重复/金额差异数据 | 自动识别与处置正确 |
| 安全测试 | 越权（横向/纵向）、SQL 注入、XSS、CSRF、限流、密码策略 | 上线前渗透测试清单闭环 |

CI 流水线：lint → typecheck → unit → build → integration → E2E（staging）→ 部署生产（手动确认）。数据库迁移独立流水线，先于应用发布且兼容回滚（只加不改原则，破坏性变更分两次发布）。

## 15. 部署与运维（R4）

- **环境**：`dev`（本地 Compose 全套）/ `staging`（生产同构 + mock 网关 + demo 供应模块）/ `production`。
- **生产拓扑（初期）**：单机或双机 Docker Compose：Nginx/Caddy（TLS）→ portal/admin/www（静态 Node 服务）+ api ×2 + worker ×1 + MySQL 8 + Redis 7；后续平滑迁 K8s。
- **配置管理**：`.env` 按环境注入，密钥不进仓库；`docker/secrets` 或部署平台密钥管理。
- **备份**：MySQL 每日全量（mysqldump/XtraBackup）+ binlog 增量，保留 30 天，异地（对象存储）归档；恢复演练每季度一次；Redis 仅作缓存与队列，不存不可再生数据。
- **监控告警**：健康检查探针、进程存活、队列积压、死信数量、磁盘/内存、MySQL 慢查询、网关成功率、短信余额；告警分级（P0 资金/开通故障电话+IM，P1 IM，P2 日报）。
- **日志**：pino JSON → 文件 + 可选 Loki；按 `requestId`/`userId` 贯穿追踪；敏感字段脱敏由 logger 层强制。
- **发布**：GitHub Actions 构建镜像 → staging 自动部署 → 生产手动审批；数据库迁移纳入发布序列并支持前向修复。

## 16. 里程碑规划

| 阶段 | 周期（估） | 交付物 | 出口标准 |
| --- | --- | --- | --- |
| M0 基建 | 1~2 周 | Monorepo 改造（官网迁入 `apps/www` 且行为不变）、CI/CD、Docker 环境、DB 迁移流水线、认证骨架、OpenAPI 脚手架 | 官网经迁移后生产部署无回归；api/portal/admin 骨架互通 |
| M1 MVP（可上线售卖） | 4~6 周 | F1 账户全项 P0、F3 商品、F4 订单、F5 支付宝/微信/余额、F6 账单、F7 基础自动化、F8 manual+demo 模块、F9 工单、F10 邮件短信、F12 后台核心、F14 安全 P0 | 全链路 E2E 通过；对账零差异；支付沙箱验收；渗透测试高危清零 |
| M2 自动化与体验 | 3~4 周 | http-api 供应模块、拼好机面板对接、余额自动续费、优惠码完整版、2FA、发票申请、知识库、报表、审计检索、PDF 账单 | 自动开通率 ≥ 90%；续费自动化上线；发票流程跑通 |
| M3 生态与国际化 | 持续 | Open API、事件 Webhook、Stripe/PayPal、微信登录、子账户、开票 API 对接、充值赠送、英文界面 | 按业务需要排期 |

MVP 上线策略：初期可用 `manual` 模块人工开通冷启动，同步开发自动开通，避免互相阻塞。

## 17. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 支付资质申请周期不可控（商户号、签名密钥、风控审核） | 阻塞 M1 上线 | M0 期间立即启动申请；沙箱先行开发；聚合通道仅作法务评估后的备选 |
| 云面板开通接口文档缺失或不稳定 | 自动开通无法交付 | `http-api` 通用模块降低对接成本；`manual` 模块保底人工开通 |
| 计费/资金逻辑缺陷造成资损 | 直接经济损失 | 金额整数化、幂等、事务、对账、高覆盖单测、渗透测试、双人复核（余额调整/退款） |
| 依赖第三方服务故障（短信/网关） | 通知或支付中断 | 多通道适配器可切换；支付单超时自动关闭防悬挂；告警监控 |
| 国内合规（备案/实名/发票）进度滞后 | 无法对大陆公众开放 | 合规开关可配置：实名拦截策略、节点销售范围与合规状态由审批项确认后启用 |
| Monorepo 迁移影响官网线上 | 品牌入口故障 | 迁移在独立分支完成，构建产物路径保持不变，预发验证后切换；保留快速回滚 |
| 单人维护风险 / 知识集中 | 工程可持续性 | 文档（本 PRD + ADR 架构决策记录 + 部署手册）、代码分层清晰、CI 门禁 |

## 18. 待审批项

进入开发前需确认：

- [ ] 支付通道：支付宝（当面付/电脑网站）、微信（Native/H5/JSAPI）商户主体与申请责任人；是否评估聚合通道。
- [ ] 人机验证码服务商（腾讯验证码 / 极验 / Turnstile）。
- [ ] 短信服务商（阿里云 / 腾讯云）与签名报备。
- [ ] 自动化默认参数：续费账单提前生成天数、逾期暂停 D+N、终止 D+M、支付单超时时长。
- [ ] 是否强制实名、实名核验服务商、销售区域与合规策略。
- [ ] 发票：开票主体、普票/专票范围、税务服务商与对接方式。
- [ ] 门户与后台域名规划及证书（portal.pinhaoji1.cn 等），后台是否限制访问来源。
- [ ] 拼好机云面板开通 API 的提供时间与接口契约（M2 依赖）。
- [ ] 税务处理方式（价格含税/税率）与账单编号规则。
- [ ] 余额政策：充值赠送是否首版支持、余额是否设有效期（建议不设）。
- [ ] 生产部署目标（云厂商、地域、规格）与备份异地存储位置。
- [ ] 官网迁移至 `apps/www` 的发布窗口与回滚预案。
- [ ] 渗透测试执行方（内部/第三方）与时间。

## 19. 术语表

| 术语 | 含义 |
| --- | --- |
| WHMCS | 国际主流的主机销售与自动化计费商业软件，本系统的对标参考 |
| Portal / 客户门户 | 客户自助操作的 Web 界面 |
| Admin / 管理后台 | 运营方管理界面 |
| 服务（Service） | 客户订购的一个商品实例（一台云服务器等），对应 WHMCS 的 Hosting Service |
| 账单（Invoice） | 应收款项凭证，支付对象 |
| 开票申请（Fapiao Request） | 中国增值税发票申请流程，与账单解耦 |
| 供应模块（Provisioning Module） | 支付后自动交付资源的外挂模块，对应 WHMCS 的 Server Module |
| 余额（Credit） | 客户预充值形成的账户余额，双式账本记录 |
| prorata | 升降配时按剩余周期比例折算差价的计费方式 |
| 支付单（Payment Intent） | 一次支付行为的独立凭证，与账单一对一或多对一 |
| 死信（Dead Letter） | 重试耗尽后进入人工处理队列的任务 |

---

## 20. 审批结论格式

审批时可直接回复：

```text
PRD-billing v0.1：批准 / 有条件批准 / 退回修改
支付通道：
短信与验证码服务商：
自动化默认参数：
实名与合规策略：
发票方案：
部署目标：
必须增加：
必须删除：
其他修改：
```
